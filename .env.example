# Example Environment Variables
# Copy this file to create environment-specific configs
# NEVER commit actual secrets - this file serves as documentation only

# API Configuration
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_LOG_LEVEL=warn

# Database (store credentials in secure vault, not in code)
DATABASE_URL=postgres://user:password@host:5432/dbname

# Third-party API Keys (public keys only)
NEXT_PUBLIC_STRIPE_KEY=pk_live_your_public_key_here

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_DEBUG_MODE=false

# Secret Keys (NEVER commit - load from GitHub Secrets or cloud vault)
JWT_SECRET=your-jwt-secret-key-change-in-production
REFRESH_TOKEN_SECRET=your-refresh-token-secret-key-change-in-production
# STRIPE_SECRET_KEY=sk_live_your_secret_key_here
# JWT_SECRET=your_jwt_secret_here
# DB_ADMIN_PASSWORD=your_db_password_here

# AWS S3 (Free Tier) — for photo uploads
# Create an IAM user with s3:PutObject on your bucket, then paste keys here
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-segregate-bucket-name
AWS_ACCESS_KEY_ID=AKIAxxxxxxxxxxxxxxxx
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# SendGrid Email Service (for transactional emails)
# Sign up at https://sendgrid.com and create an API key with Mail Send permissions
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SENDGRID_FROM_EMAIL=noreply@segregate.app
SENDGRID_FROM_NAME=SegreGate

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
